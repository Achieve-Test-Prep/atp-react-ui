import React from 'react';

import ReactDOM from 'react-dom/client';
import '../style/atp-css-styles.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <div className="p-4">
      <h1 className="mb-4 text-2xl font-bold">Component Library Dev Mode</h1>
      <p>Use this space to test your components in development mode.</p>
    </div>
  </React.StrictMode>
);
